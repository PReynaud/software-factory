# Cloudflare adapter (stub)

Not implemented in v1. `FACTORY_DEPLOY=cloudflare` must fail closed.

Intended later: Workers + static assets, MCP `https://mcp.cloudflare.com/mcp`, `wrangler`, CNAME from OVH to the workers.dev / pages.dev hostname.
