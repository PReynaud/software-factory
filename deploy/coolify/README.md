# Coolify adapter (stub)

Not implemented in v1. `FACTORY_DEPLOY=coolify` must fail closed.

Intended later: Coolify on an OVH VPS, wildcard `*.pierre-reynaud.fr`, REST API. Useful when many apps share one Node runtime.
